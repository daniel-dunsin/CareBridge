import { motion } from "framer-motion";
import { opacityVariant } from "@/lib/utils/variants";

const ReviewInfo = () => {
  return <motion.div {...opacityVariant}>reviews</motion.div>;
};

export default ReviewInfo;
