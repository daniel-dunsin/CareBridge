import { opacityVariant } from "@/lib/utils/variants";
import { motion } from "framer-motion";

const Doctors = () => {
  return <motion.div {...opacityVariant}>Doctors</motion.div>;
};

export default Doctors;
