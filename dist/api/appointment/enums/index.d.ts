export declare enum AppointmentStatus {
    PENDING = "pending",
    SUCCESSFUL = "successful",
    FAILED = "failed",
    CANCELLED = "cancelled"
}
export declare enum AppointmentMode {
    ONLINE = "online",
    PHYSICAL = "physical"
}
export declare const DiagnosisRef: {
    BONE_METRICS: string;
    BRAIN_METRICS: string;
    EYES_METRICS: string;
    HEART_METRICS: string;
    KIDNEY_METRICS: string;
    LIVER_METRICS: string;
    SKIN_METRICS: string;
    TEETH_METRICS: string;
};
